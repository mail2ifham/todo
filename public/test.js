const arrayList = ["a", "b", "c", "d", "e", "f"];

// arrayList.map((index,latter) => console.log(index,latter))

console.log(
  new Date().toLocaleDateString("en-us", { weekday: "long",month: "short",day: "numeric",})
);
